import React from 'react';
import {
    Image
} from './styled';

export default class GalleryCard extends React.PureComponent {
    constructor(props) {
        super(props);

        // this.state.GalleryCard
    }

    render() {
        return (
            <Image.Card
        )
    }
}